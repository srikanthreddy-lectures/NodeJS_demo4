{
	"name": "liveservtest",
	"version": "1.0.0",
	"description": "",
	"main": "index.html",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"serve": "live-server ./public",
		"prepare": "npx crlf --set=LF",
		"start": "npm run serve"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"live-server": "^1.2.2",
		"nodemon": "^2.0.19"
	}
}
